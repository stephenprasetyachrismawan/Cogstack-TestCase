window.PanelRegistry=window.PanelRegistry||{};window.PanelRegistry.docker={init(){const c=window.APP_DATA.dockerChecklist;document.getElementById('dockerRoot').innerHTML=c.map((x,i)=>`<div class="checklist-item"><span>${x}</span><span><button class="btn" data-k="${i}" data-v="ada">ada</button><button class="btn" data-k="${i}" data-v="partial">partial</button><button class="btn" data-k="${i}" data-v="tidak">tidak</button></span></div>`).join('')+`<textarea placeholder="Startup sequence"></textarea><textarea placeholder="Runbook followability assessment"></textarea>`;document.getElementById('dockerRoot').onclick=e=>{const b=e.target.closest('button[data-k]');if(!b)return;window.Store.setChecklist('docker_'+b.dataset.k,b.dataset.v);};window.Store.markDone('docker');}};